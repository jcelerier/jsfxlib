desc:Tevio Auto Mute Recording State
Author: Tevio
version: 2018/06/26
changelog: Initial release
Author URI: 
Repository: 
Repository URI:


slider1:1<0,1,1{Disable,Enable}>ON
in_pin:input 1(L)
in_pin:input 2(R)
out_pin:Main out(L)
out_pin:Main out(R)


@sample

slider1 ? (
    play_state>=5 ? (spl0=0; spl1=0);
    );